[build]
dockerfilePath = "juntos-alpha/Dockerfile"
context = "."

[deploy]
startCommand = "java -jar app.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 120
healthcheckInterval = 10
restartPolicyType = "ON_FAILURE"
numReplicas = 1

[deploy.environment]
SERVER_ADDRESS = "0.0.0.0"