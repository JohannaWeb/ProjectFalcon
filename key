import React from 'react';
import { List } from '@material-ui/core';

const ChannelList = () => {
  const channels = [...]; // assume this is your list of channels

  return (
    <List>
      {channels.map((channel) => (
        <li key={channel.id}>
          {/* render channel information */}
        </li>
      ))}
    </List>
  );
};
